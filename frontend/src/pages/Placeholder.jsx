export function Placeholder({ title }) {
  return (
    <div>
      <h1 className="text-xl font-semibold">{title}</h1>
      <p className="mt-2 text-sm text-slate-500">Not built yet — scaffolded route only.</p>
    </div>
  );
}
