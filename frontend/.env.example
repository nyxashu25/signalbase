# No frontend env vars yet — /api is proxied to the backend by vite.config.js
# in dev, and served from the same origin in production. Add VITE_-prefixed
# vars here if/when the frontend needs build-time config.
