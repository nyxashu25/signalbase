// Models land in Phase 01 (data model, auth & tenancy). This stub exists so
// `prisma generate` / `prisma migrate` work from the very first commit.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
